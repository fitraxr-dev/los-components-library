## Penggunaan Debtor Detail Section

Section Debtor detail in stepper debtor information on every modul can use component DebtorDetailSectionV2 with props debtorInfoData={data debtor}
for example:

```
import DebtorDetailSectionV2 from '@/components/shared/SmiSection/DebtorDetailSectionV2/DebtorDetailSectionV2';

      <DebtorDetailSectionV2 debtorInfoData={debtorInfoData} />
```